TYL 便携版
===========

1. 请先完整解压此压缩包，再运行 TYL.exe；不要直接在压缩包内启动。
2. TYL 默认驻留系统托盘。选中文字后按 Alt+T 翻译。
3. settings.json 和 tyl.log 会自动生成在本目录，请保留整个文件夹以维持配置。
4. 升级时先从托盘退出 TYL，保留 settings.json，然后替换 TYL.exe。
5. 分享程序时不要附带自己的 settings.json 或 tyl.log，其中可能包含私人配置。

系统要求：Windows 10 / 11 x64，以及 WebView2 Runtime。
